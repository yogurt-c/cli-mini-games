# 블랙잭 온라인 게임 배포 가이드

## 📋 배포 개요

- **서버 PC**: 1대 (게임 서버 실행)
- **클라이언트 PC**: 여러 대 (플레이어들)

---

## 🖥️ 서버 PC 설정

### 1. 서버 코드 준비
서버 PC에 다음 파일들을 복사:
```
blackjack_online/
├── server.py
├── game_logic.py
└── requirements.txt
```

### 2. Python 환경 구성
```bash
# Python 3.7 이상 필요
python --version

# 의존성 설치
pip install -r requirements.txt
```

### 3. 서버 실행
```bash
python server.py
```

실행되면:
```
==================================================
블랙잭 온라인 서버 시작
포트: 8000
==================================================
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 4. 서버 IP 확인

**Windows:**
```bash
ipconfig
# IPv4 주소 확인 (예: 192.168.1.100)
```

**Mac/Linux:**
```bash
ifconfig
# inet 주소 확인 (예: 192.168.1.100)
```

### 5. 방화벽 설정

**Windows 방화벽:**
1. 제어판 → Windows Defender 방화벽
2. 고급 설정 → 인바운드 규칙
3. 새 규칙 → 포트 → TCP → 8000 허용

**Mac 방화벽:**
```bash
# 시스템 설정 → 보안 및 개인 정보 보호 → 방화벽
# Python 허용
```

**Linux (ufw):**
```bash
sudo ufw allow 8000/tcp
```

---

## 💻 클라이언트 PC 배포

### 방법 1: 실행 파일 배포 (추천) ⭐

#### Step 1: 서버 PC에서 클라이언트 빌드
```bash
cd blackjack_online

# Windows
build.bat

# Mac/Linux
./build.sh

# 또는 Python 스크립트
python build_client.py
```

#### Step 2: 실행 파일 복사
빌드 완료 후 `dist/` 폴더에서 실행 파일 복사:

**Windows:**
- `dist/blackjack_client.exe` (약 10-15MB)

**Mac/Linux:**
- `dist/blackjack_client` (약 10-15MB)

#### Step 3: 클라이언트 PC에 전달

**방법 A: USB 메모리**
1. USB에 실행 파일 복사
2. 클라이언트 PC에 붙여넣기

**방법 B: 네트워크 공유**
1. 서버 PC에서 폴더 공유 설정
2. 클라이언트 PC에서 네트워크 드라이브로 접근하여 복사

**방법 C: 파일 전송 (Python 간단 서버)**
```bash
# 서버 PC의 dist 폴더에서
python -m http.server 9000

# 클라이언트 PC 브라우저에서
# http://서버IP:9000 접속 후 다운로드
```

#### Step 4: 클라이언트 실행

**Windows:**
```
blackjack_client.exe
```
또는 더블클릭

**Mac/Linux:**
```bash
# 실행 권한 부여
chmod +x blackjack_client

# 현재 디렉토리에서 실행 (./ 필수)
./blackjack_client
```

실행하면 서버 주소와 이름 입력:
```
서버 주소 입력 (예: ws://192.168.1.100:8000): ws://192.168.1.100:8000
플레이어 이름 입력: Player1
```

---

### 방법 2: Python 스크립트 배포

Python이 설치된 PC라면 스크립트로도 가능합니다.

#### Step 1: 클라이언트 파일 복사
```
client.py
```

#### Step 2: 의존성 설치
```bash
pip install websockets
```

#### Step 3: 실행
```bash
python client.py ws://192.168.1.100:8000 Player1
```

---

## 🎮 게임 시작하기

### 1. 서버 시작
서버 PC에서:
```bash
python server.py
```

### 2. 플레이어 1 접속
첫 번째 클라이언트 실행:
```
서버 주소: ws://192.168.1.100:8000
플레이어 이름: Alice
```
→ "상대방을 기다리는 중..." 메시지 표시

### 3. 플레이어 2 접속
두 번째 클라이언트 실행:
```
서버 주소: ws://192.168.1.100:8000
플레이어 이름: Bob
```
→ 자동으로 게임 시작!

---

## 🔧 문제 해결

### 연결 실패 (Connection Refused)
**원인:**
- 서버가 실행되지 않음
- 서버 IP 주소 오류
- 방화벽 차단

**해결:**
1. 서버 PC에서 `python server.py` 실행 확인
2. IP 주소 다시 확인 (ipconfig/ifconfig)
3. 방화벽 8000 포트 허용 확인
4. 같은 네트워크(내부망)에 있는지 확인

### 서버 IP 찾기
```bash
# Windows
ipconfig | findstr IPv4

# Mac/Linux
ifconfig | grep inet
```

### 포트 변경이 필요한 경우
`server.py` 마지막 줄 수정:
```python
uvicorn.run(app, host="0.0.0.0", port=8000)  # 8000 → 원하는 포트
```

클라이언트 접속 시:
```
ws://서버IP:새포트번호
```

---

## 📦 빠른 배포 체크리스트

### 서버 PC ✅
- [ ] Python 3.7+ 설치
- [ ] 서버 파일 복사 (server.py, game_logic.py, requirements.txt)
- [ ] `pip install -r requirements.txt`
- [ ] `python server.py` 실행
- [ ] IP 주소 확인 (ipconfig/ifconfig)
- [ ] 방화벽 8000 포트 허용

### 클라이언트 PC ✅
- [ ] 실행 파일 받기 (blackjack_client.exe 또는 blackjack_client)
- [ ] 실행 후 서버 주소 입력 (ws://서버IP:8000)
- [ ] 플레이어 이름 입력
- [ ] 게임 시작!

---

## 💡 팁

1. **서버는 한 번만 실행**: 여러 게임이 동시에 가능 (2명씩 매칭)
2. **고정 IP 권장**: 서버 PC의 IP를 고정으로 설정하면 편리
3. **자동 시작 스크립트**: 서버를 자주 키는 경우 시작 스크립트 작성
4. **실행 파일 배포**: Python 없는 환경에서도 실행 파일 사용
